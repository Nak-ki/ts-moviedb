import {MovieList} from "../components/MovieList";
import {MovieInfo} from "../components/MovieInfo/MovieInfo";

const MoviesPage = () => {
    return (
        <div>
            <MovieList/>
        </div>
    );
};

export {MoviesPage};
